export type ColorScheme =  {
    backgroundColor: string;
    borderColor: string;
    minScore: number;
  }
  

export type RatingColorScheme = {
    backgroundColor: string;
    borderColor: string;
    textColor: string;
    minScore: number;
}
